package  {		import flash.display.MovieClip;			public class Pro extends Soldier{				var headShotProb : int;				public function Pro(game : Main, map : Map) {			super(game, map);			this.shootingRate = 40;						if(generateRandomInt(10, 0) > 5) {				this.damage = 2;			} else {				this.damage = 3;			}		}				public function generateRandomInt(max : int, min : int) : int {			return min + (max - min) * Math.random();		}	}	}