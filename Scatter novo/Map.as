package  {		public class Map {		var walkableMap : Vector;		var mapSize;				public function Map() {					}		public function generateMap(level : Level) {			walkableMap = new Vector<Vector<Boolean>>(level.mapSize);			for (var i = 0; i < level.mapSize; i++) {				walkableMap[i] = new Vector<Boolean>(level.mapSize);			}						for(var i = 0; i < level.objects.length; i++) {				setWalkable(level.objects[i]);			}		}		public function setWalkable(object : MapObjects) {			for(var i = 0; i < object.sizeX; i++) {				walkableMap[posX + i][posY] = object.walkable;			}						for(var i = 0; i < object.sizeY; i++) {				walkableMap[posX][posY + i] = object.walkable;			}		}		public function drawInMap(object : MapObjects) {					}				public function createMapObject() {					}			}	}